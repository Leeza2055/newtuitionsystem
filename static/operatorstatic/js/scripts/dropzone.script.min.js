"use strict";

$(document).ready(function () {
  Dropzone.options.singleFileUpload = {
    paramName: "file",
    maxFiles: 1,
    init: function init() {
      this.on("maxfilesexceeded", function (file) {
        this.removeAllFiles();
        this.addFile(file);
      });
    }
  };
  
});